project:
  name: qbmake
components:
  basic:
    context: basic
    deps:
      include: '**/*'
      ignore: ['Dockerfile*.log', 'Dockerfile*.built']
  multi-container:
    containers:
      php-fpm:
      nginx:
